# One-Time Task: GitHub PAT Expiry Reminder

This is a reminder, not an action to perform. When the daily digest task picks up this file (per the one-time task file mechanism in `prompts/daily.md`), just fold the text below into the digest's `one-time-tasks` section as-is — don't attempt any git operations beyond deleting this file, and don't try to rotate the token yourself.

**Item title:** GitHub Token Expiring Soon
**Item body (use as the result/summary for this item):**

The fine-grained PAT "Claude Scheduled Tasks on Air-Server" (scoped to `jghaines/jgh-claude-public`) expires **2027-01-02** — one week from today. Once it expires, `daily-morning-digest`, `weekly-digest-recap`, and `monthly-b8-mcp-search` will all fail to push and the digest will stop updating.

To fix: generate a new fine-grained PAT at github.com/settings/tokens, scoped only to `jghaines/jgh-claude-public`, with the same three permissions (Contents: read/write, Metadata: read-only, Pages: read/write), then give it to Claude in a live chat session so it can update it in the persistent repo's git remote URL (`~/Developer/github.com/jghaines/jgh-claude-public/.git/config`) and the fallback clone commands embedded in all three scheduled tasks.
